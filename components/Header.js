const Header = () => {
  return (
    <header className="absolute top-0 w-screen px-6  flex flex-row items-center justify-start">
      <p className="font-QS text-[62px] text-secondary-100 sm:opacity-0 select-none cursor-pointer transition-all duration-300 hover:text-primary">
        typo
      </p>
    </header>
  );
};

export default Header;
